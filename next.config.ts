import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['upload.wikimedia.org', 'avatars.githubusercontent.com', 'vehicle-images.dealerinspire.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
